type Query {
  user(id: ID!): User
}

type Mutation {
  login(input: AuthInput): User
  signup(name: String!, input: AuthInput): User
}

type User {
  id: ID!
  name: String!
  email: String!
}

input AuthInput {
  email: String!
  password: String!
}
